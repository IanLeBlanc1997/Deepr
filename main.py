#need to make database to keep track of all user information. A simple JSON file would probably do the trick, but a SQL database would probably scale better?

thesis = input('What is your thesis?\n')
assets = []
